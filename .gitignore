# Dependencies (none needed for this static site, but just in case)
node_modules/

# Vercel
.vercel/

# Private — NEVER commit secrets / passwords
*-PRIVATE.txt
NEP-passwords-PRIVATE.txt

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Large source headshots (cropped versions are committed instead)
assets/*_headshot.jpg
